// pop-up message type
enum POP_UP_MESSAGE_TYPE { success, error, warning, confirm, none }

// vertical bar button type
enum ACTION_BUTTON_TYPE { add, view, update, delete }

// bloc state status
enum BLOC_STATE_STATUS { initial, loading, success, failure }

// fab status
enum FAB_STATUS { hideFAB, defaultFAB, centerFAB }
